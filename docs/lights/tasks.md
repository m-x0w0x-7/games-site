# tasks.md

## 1. プロジェクト準備

- [x] `index.html` を作成する
- [x] `style.css` を作成する
- [x] `main.js` を作成する
- [x] `index.html` で `style.css` と `main.js` を読み込む
- [x] ブラウザでページが表示されることを確認する

## 2. HTMLを作る

- [x] ゲーム全体を囲む要素を作成する
- [x] 手数表示用の要素を作成する
- [x] 5×5の盤面を表示する要素を作成する
- [x] リセットボタンを作成する
- [x] クリアメッセージ用の要素を作成する

## 3. 盤面データを作る

- [x] 盤面サイズ `5` を定数で定義する
- [x] 25個分のマス状態を配列で管理する
- [x] `true` を点灯、`false` を消灯として扱う
- [x] すべて点灯状態の初期配列を作成する

## 4. 盤面を描画する

- [x] 盤面要素を取得する
- [x] 盤面要素の中身を空にする処理を作る
- [x] 配列の数だけボタン要素を作成する
- [x] 点灯状態のマスに `is-on` クラスを付ける
- [x] 消灯状態のマスには `is-on` クラスを付けない
- [x] 作成したマスを盤面要素に追加する
- [x] 25個のマスが表示されることを確認する

## 5. 見た目を整える

- [x] 盤面をCSS Gridで5列にする
- [x] 各マスを正方形にする
- [x] 点灯状態と消灯状態の色を分ける
- [x] マス同士の間隔を設定する
- [x] ボタンのデフォルト装飾を消す

## 6. 反転処理を作る

- [x] 指定したindexのマスを反転する関数を作成する
- [x] タップしたマス自身を反転対象にする
- [x] 上のマスを反転対象にする
- [x] 下のマスを反転対象にする
- [x] 左のマスを反転対象にする
- [x] 右のマスを反転対象にする
- [x] グリッド外のマスは反転しない
- [x] 左端のマスで、前の行の右端が反転しないことを確認する
- [x] 右端のマスで、次の行の左端が反転しないことを確認する

## 7. マスをタップできるようにする

- [x] 各マスにクリックイベントを設定する
- [x] クリックされたマスのindexを取得する
- [x] クリック時に反転処理を実行する
- [x] クリック後に盤面を再描画する
- [x] クリックしたマスと上下左右だけが反転することを確認する

## 8. 手数を管理する

- [x] 手数用の変数を作成する
- [x] 初期値を `0` にする
- [x] マスをクリックしたら手数を1増やす
- [x] 手数表示を更新する関数を作成する
- [x] 画面に現在の手数が表示されることを確認する

## 9. クリア判定を作る

- [x] すべてのマスが点灯しているか判定する関数を作成する
- [x] マスをクリックしたあとにクリア判定を実行する
- [x] すべて点灯していたらクリア状態にする
- [x] クリアメッセージを表示する
- [x] クリア後はマスをクリックできないようにする

## 10. 初期盤面を生成する

- [x] すべて点灯状態の盤面を作成する
- [x] ランダムに複数のマスを選ぶ
- [x] 選ばれたマスに反転処理を実行する
- [x] 生成された盤面を現在の盤面として保存する
- [x] 生成された盤面を初期盤面として保存する
- [x] 初期盤面が必ず解ける作り方になっていることを確認する

## 11. リセット処理を作る

- [x] リセットボタンにクリックイベントを設定する
- [x] 現在の盤面を初期盤面に戻す
- [x] 手数を `0` に戻す
- [x] クリア状態を解除する
- [x] クリアメッセージを非表示にする
- [x] 盤面と手数表示を更新する

## 12. 最終確認

- [x] 初回表示時に5×5の盤面が表示される
- [x] マスをクリックすると正しく反転する
- [x] 手数が増える
- [x] すべて点灯するとクリアになる
- [x] クリア後にマスをクリックできない
- [x] リセットで初期状態に戻る
- [x] リセット後に再プレイできる
